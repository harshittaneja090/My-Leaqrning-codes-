#include<stdio.h>
#include<string.h>
main()
{
	char arr[10];
	 char arr1[30],s;
	 char nm[10];
	 printf("enter your name:");
	 gets(nm);
	 printf("\nenter your address:");
	 gets(arr1);
	 printf("\nenter your phone number:");
	 gets(arr);
	 printf("your name is :%s ",arr);
	 printf("\nyour phone number is:%d",arr);
	 printf("\n your address is%s",arr1);
	
}
